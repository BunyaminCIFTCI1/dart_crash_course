// ignore_for_file:unused_local_variable

void main(List<String> args) {
  const names = ["emir", "ben", "baran", "hakan"];
  // where returns us an Iterable and it accepts a function whose return type is bool
  // final it = names.where((name) => name.startsWith("e")).toList();
  // sublist method takes two parameters to start and finish and to sublist the original list
  // final foo = names.sublist(1);
  // forEach method's return type is void and it executes the action you want for each element separately
  // names.forEach((element) {
  //   print(element.toUpperCase());
  // });
  // names.forEach(print);
}
