// ignore_for_file: unused_local_variable

void main(List<String> args) {
  // the difference between map and where is return type of where method is bool
  // map is like combination of both forEach and where methods

  // const names = ["ben", "hakan", "emre", "baran"];
  // final foo = names.map((e) {
  //   return e.toLowerCase();
  // });

  // const numbers = [1, 4, 2, 6, 7];
  // final sum = numbers.fold(
  //     10, (previousValue, currentValue) => previousValue + currentValue);

  // in the code below we find all the length of the strings in the list
  // remember that initialvalue is firstly becoming previous value then it continues like that

  // final totalLength =
  //     names.fold(0, (previousValue, str) => previousValue + str.length);
  // print(totalLength);
}
