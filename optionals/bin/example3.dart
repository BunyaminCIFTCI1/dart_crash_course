// ignore_for_file: unused_local_variable

void main(List<String> args) {
  //code below gives an error because dart thinks that the first element of list is a String but it is not
  // final List<String> list = [];

  // final foo = list.first;
  // print(foo);
// even if we do it like below it gives an error because foo is not null it is String but actually it is not so it is like a built in error for dart
  // final list = [];
  // final String? foo = list.first;
  // if (foo == null) {
  //   print("it is null");
  // }
}
