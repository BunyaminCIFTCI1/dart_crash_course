void main(List<String> args) {
  // to get all the cases of an enum use below getter
  // print(AnimalType.values);
  //below we have some other useful methods
  // print(AnimalType.cat.index);
  // print(AnimalType.cat.name);
}

enum AnimalType {
  dog,
  rabbit,
  cat,
  monkey,
}
